node_modules
**/dist
**/.next
**/.turbo
pnpm-lock.yaml
# Local / private working area (git-ignored; backed by a separate private repo).
/local
# Claude Code memory (remember plugin) — git-ignored, per-machine scratch state.
.remember
runtime/app/(platform)/(plugins)
runtime/app/(minimal)
runtime/generated
runtime/public/sw.js
# Storybook generated config and static output
packages/ui/.storybook
packages/ui/storybook-static
