# End-to-end tests

Browser-driven end-to-end tests (Playwright). Not yet populated — reserved for
future tasks. Tests here should be named `*.e2e.ts` or `*.spec.ts` and require a
running instance of the full stack.
