# Integration tests

Cross-service integration tests (e.g. runtime ↔ auth server over HTTP). Not yet
populated — reserved for future tasks. Tests here should be named
`*.integration.test.ts` and may require running services (set up in CI via Docker
Compose).
