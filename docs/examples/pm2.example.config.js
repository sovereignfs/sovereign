// PM2 ecosystem config for Sovereign — generated by `sv setup pm2`.
// Run: pm2 start ecosystem.config.js
// Persist across reboots: pm2 startup && pm2 save
//
// This file shows the default output of `sv setup pm2 --dir /opt/sovereign`.
// Generate a config tailored to your install path:
//
//   pnpm sv setup pm2 --dir /opt/sovereign --env-file /opt/sovereign/.env
//
// See docs/self-hosting.md for the full non-Docker deployment guide.

module.exports = {
  apps: [
    {
      name: 'sovereign-auth',
      script: '/opt/sovereign/apps/auth/.next/standalone/apps/auth/server.js',
      instances: 1,
      autorestart: true,
      watch: false,
      env: {
        NODE_ENV: 'production',
        PORT: '3001',
        // Bind auth only on loopback — it is not intended to be publicly reachable.
        HOSTNAME: '127.0.0.1',
      },
    },
    {
      name: 'sovereign-runtime',
      script: '/opt/sovereign/runtime/.next/standalone/runtime/server.js',
      instances: 1,
      autorestart: true,
      watch: false,
      env: {
        NODE_ENV: 'production',
        PORT: '3000',
        HOSTNAME: '0.0.0.0',
      },
    },
  ],
};
